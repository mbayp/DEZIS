<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GEEKS_DEZIS" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GEEKS_DEZIS" parent="Base.Theme.GEEKS_DEZIS" />

    <style name="Divider">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@android:color/black</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:layout_marginStart">11.5dp</item>
        <item name="android:layout_marginEnd">9dp</item>
    </style>

</resources>